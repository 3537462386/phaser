## 2026-05-30

### 项目启动崩溃修复
- **发现**：游戏无法启动，主因是 `player_down.png` spritesheet 动画帧数不匹配
  - `player_down.png` 实际只有3帧（102×378，每帧126px高），但预载使用了 `endFrame: 3`（4帧），动画使用了 `end: 7`（8帧）
  - 启动时不会崩溃，但玩家死亡触发播放爆炸动画时崩溃
- **修复**：
  1. `js/scenes/GameScene.js` 第43行：`endFrame: 3` → `endFrame: 2`（匹配实际3帧）
  2. `js/entities/Player.js` 第197行：`end: 7` → `end: 2`（匹配实际3帧：0,1,2）
- 其余28个JS文件代码逻辑审查未发现其他崩溃级bug
